import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";
import { Providers } from "@/components/providers";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Nathan's Physics MCQ - Class 12 Physics Quiz App",
  description: "Comprehensive Class 12 NCERT Physics MCQ quiz application with chapter-wise tests, progress tracking, and certificate generation.",
  keywords: ["Physics", "MCQ", "Class 12", "NCERT", "Quiz", "Physics Questions"],
  authors: [{ name: "Nathan's Physics MCQ" }],
  openGraph: {
    title: "Nathan's Physics MCQ",
    description: "Class 12 NCERT Physics MCQ Quiz App",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        <Providers>
          {children}
          <Toaster />
        </Providers>
      </body>
    </html>
  );
}
